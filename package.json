{
  "name": "reptracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"wrangler dev --port 8787\"",
    "build": "vite build",
    "build:worker": "esbuild src/worker/index.ts --bundle --outfile=dist/worker.js --format=esm --target=es2022 --minify",
    "deploy": "npm run build && npm run build:worker && wrangler deploy",
    "db:migrate:local": "wrangler d1 execute reptracker-db --local --file=migrations/0000_initial.sql",
    "db:migrate:remote": "wrangler d1 execute reptracker-db --file=migrations/0000_initial.sql",
    "db:seed:dump": "node scripts/dump-names.mjs",
    "db:supersu:local": "node scripts/create-supersu.mjs | wrangler d1 execute reptracker-db --local --stdin",
    "db:supersu:remote": "node scripts/create-supersu.mjs | wrangler d1 execute reptracker-db --stdin",
    "db:seed:generate": "node scripts/generate-catalog-seed.mjs > migrations/0001_seed_catalog.sql",
    "db:seed:local": "npm run db:seed:generate && wrangler d1 execute reptracker-db --local --file=migrations/0001_seed_catalog.sql",
    "db:seed:remote": "npm run db:seed:generate && wrangler d1 execute reptracker-db --file=migrations/0001_seed_catalog.sql"
  },
  "dependencies": {
    "drizzle-orm": "^0.36.0",
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "pinia": "^2.2.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "@vitejs/plugin-vue": "^5.1.0",
    "autoprefixer": "^10.4.0",
    "concurrently": "^9.0.0",
    "drizzle-kit": "^0.28.0",
    "esbuild": "^0.24.0",
    "postcss": "^8.4.0",
    "tailwindcss": "^3.4.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "wrangler": "^4.76.0"
  }
}
